node_modules/
dist/
dist-**/
# 前端构建产物：源码在 web/，由 `cd web && npm run build` 输出并覆盖此目录
public/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
**/*.log


# Editor directories and files
.vscode/settings.json
.idea
.codebuddy
*.suo
*.ntvs*
*.njsproj
*.sln
*.local
*.sw?
dist.zip
data/*
src/data/*
test/data/*
release/

# 多包管理器共存：权威锁文件是 package-lock.json，
# bun / pnpm 在本机安装时会重新生成各自的锁文件，忽略以免误提交
bun.lock
pnpm-lock.yaml
pnpm-workspace.yaml

# 个人本机脚本
update.bat

# 本地私有配置：含源库绝对路径 / IP 白名单 / 反代信任等，永不提交；
# 公共模板见 config.example.js（部署时 cp config.example.js config.js 再按需修改）
config.js